[project]
name = "hosts-page-blocker"
version = "0.1.0"
requires-python = ">=3.6"

[tool.uv]
dev-dependencies = [
    "ruff>=0.8.4",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I"]
